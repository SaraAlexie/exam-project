import StyledHeading from "../styles/StyledHeading";
import SectionTwo from "./SectionTwo";

const Events = () => {
    return (
        <section>
            <StyledHeading title="events of the month" />
            <SectionTwo />
        </section>
    );
};

export default Events;
